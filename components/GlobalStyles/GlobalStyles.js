import {Global, css} from '@emotion/core'

const GlobalStyles = () => (
    <>
        <Global styles={`
            body{
                color: #f30;
        `}/>
    </>
)

export default GlobalStyles